# blockAI

統合版(26.51+)アドオン。AI村人20人に職業を与え、村長として村と共有倉庫を育てる。安定版Script APIのみ。コードは `packs/BP/scripts`、見た目は `tools/gen-*.mjs` で生成。変更したら版を上げ `npm run check` 後にpush。テスト用zip更新時は最後に「テストで確認すること」を示し、未回答分は次回「前回未回答」と載せる。詳細は `.claude/rules/`。
