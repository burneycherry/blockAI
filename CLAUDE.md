# blockAI

Minecraft統合版(26.51+)用のアドオン。AI村人に職業を与え、村長として村を発展させる。安定版Script APIのみ使用。スクリプトは `packs/BP/scripts` の `core/`（共通）と `jobs/`（1職業1ファイル）。変更したら版を上げ、`npm run check` を通してからpushする。詳細は `.claude/rules/`。

テスト用のデータ（テスト用 zip）を更新したら、チャットの最後に「テストで確認すること」とその内容を表示する。
